//Difference between the 'var' and 'let' and 'const' keyword
/*
var : Scope is local or global. Declare same varibale usin 'var' Not any error.
let : scope is block Scope(like function). Declare same varibale usin 'let' fasing runtime error.
const : It's only redable. Not change entire code.
*/

//The Object.freeze() method freezes an object. A frozen object can no longer be changed; freezing an object prevents new properties from being ...